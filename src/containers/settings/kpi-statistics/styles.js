import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  chartTitle: {
    fontFamily: 'Helvetica',
    fontSize: 10,
    fontWeight: 'bold',
    textAlign: 'center',
    paddingTop: 12
  }
})
